const intialState ={

    users:[],
    user:{},
    loading : false,};

    const usersReducers = (state =  intialState, action)=>{

        switch(action.type){
            default :
            return state;
        }
    }

    export default usersReducers;